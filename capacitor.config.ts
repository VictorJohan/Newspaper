import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.newspaper',
  appName: 'Newspaper',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
